input {
    jdbc {

        jdbc_driver_library => "/usr/share/logstash/mysql-connector-java-8.0.28.jar"
        jdbc_driver_class => "com.mysql.cj.jdbc.Driver"
        jdbc_connection_string => "jdbc:mysql://my-database:3306/mydocker02"
        jdbc_user => "root"
        jdbc_password => "root"
        schedule => "* * * * *" 
        use_column_value => true
        tracking_column => "updateat"
        tracking_column_type => "timestamp"
        last_run_metadata_path => "./post.txt"
        statement => "select post_id, contents, title, writer, hit, like_count, createat ,updateat
        from post
        where updateat > :sql_last_value 
        order by updateat asc"
        
    }

}

filter { 
    # date { 
    #     match => ["updateat","YYYY-MM-dd HH:mm:ss:SS"] 
    #     timezone => "Asia/Seoul" 
    #     target => "updateat" 
    #     }
    mutate { 
        remove_field => ["@version","@timestamp"] 
        # join => {"postTags" => ","}
        }  

}


# filter {
#      if "IMP" in [log][file][path] {
#      mutate { remove_field => ["@version"] } 
#      data { match => {"[imp][updataat]", "YYYY-MM-dd H:m:s" }, target => "@timestamp", timezone=>"Asia/Seoul" }
#      }
# }



output {
    elasticsearch {
        hosts => "elasticsearch:9200"
        index => "post"
        # document_type => "message_logs"
        document_id => "%{post_id}"
    }

}